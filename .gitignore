# Ignore Rust build files.
target/

# Even if we'd normally check in `Cargo.lock` for libraries, it doesn't make a ton of sense to give
# these out in our assignment handouts.
Cargo.lock

# For test handouts and handins.
*.tar
*.zip

# For local testing of the autograders.
results/
submission/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Ignore macOS metadata.
.DS_Store
